# bootstrap - carrega configurações antes das proprias configurações da aplicação
spring:
  application:
    name: 'loja-api'
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888